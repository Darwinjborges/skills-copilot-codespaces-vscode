type SkillsMember = {
    name: string;
    age: number;
    email: string;
}

function createSkillsMember(name: string, age: number, email: string): SkillsMember {
    return { name, age, email };
}
    return { name, age, email };
}
